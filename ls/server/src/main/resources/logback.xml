<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Status messages must never reach System.out: the LSP channel owns
         stdout and shares it with the DAP socket handshake. Any extra bytes
         before the first Content-Length frame desynchronize the client. -->
    <statusListener class="ch.qos.logback.core.status.NopStatusListener"/>
    <!-- LOG_FILE is set by the VS Code extension via -DLOG_FILE=... at launch.
         Falls back to the system temp directory when running outside the extension. -->
    <variable name="LOG_FILE" value="${LOG_FILE:-${java.io.tmpdir}/cql-ls.log}"/>

    <appender name="STDERR" class="ch.qos.logback.core.ConsoleAppender">
        <target>System.err</target>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{35} %msg %n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_FILE}.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>7</maxHistory>
            <totalSizeCap>50MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{35} %msg %n</pattern>
        </encoder>
    </appender>

    <!-- Debug logging for debug server / breakpoint handler diagnostics.
         TRACE (not DEBUG) so the bulkier opt-in diagnostics, e.g. the synthetic
         __debugEval__ function for ad-hoc eval, only reach the output channel when
         the CQL output channel is switched to Trace. -->
    <logger name="org.opencds.cqf.cql.debug" level="TRACE" />
    <!-- Debug logging for View ELM / compilation diagnostics -->
    <logger name="org.opencds.cqf.cql.ls.server.command.ViewElmCommandContribution" level="DEBUG" />
    <logger name="org.opencds.cqf.cql.ls.server.manager.CqlCompilationManager" level="DEBUG" />
    <logger name="org.opencds.cqf.cql.ls.server.provider.FederatedLibrarySourceProvider" level="DEBUG" />
    <!-- Debug logging for cross-project library resolution -->
    <logger name="org.opencds.cqf.cql.ls.server.manager.LibraryResolutionManager" level="DEBUG" />
    <logger name="org.opencds.cqf.cql.ls.server.service.FileContentService" level="DEBUG" />

    <!-- log.level is set by the VS Code extension via -Dlog.level=ALL in debug mode.
         Defaults to INFO so startup and command-lifecycle messages are visible. -->
    <root level="${log.level:-INFO}">
        <appender-ref ref="STDERR" />
        <appender-ref ref="FILE" />
    </root>
</configuration>