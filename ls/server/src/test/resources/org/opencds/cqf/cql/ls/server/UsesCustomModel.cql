library UsesCustomModel version '1'

// Declares a model that is neither bundled nor in npm, forcing the
// ContentServiceModelInfoProvider to be consulted for its model info.
using Custom version '1.0.0'

define "X":
    1
